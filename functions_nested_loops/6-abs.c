#include "main.h"

/**
 * _abs - returns absolute value of number
 * @i: integer to be input
 * Return: absolute value of integer inputted
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}

}
